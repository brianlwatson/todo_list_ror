<% require_user %>

<h1>Create a New Task</h1>

<% if @task.errors.any? %>
    <ul>
    <h2>Following Errors Occurred:</h2>
    <% @task.errors.full_messages.each do |msg| %>
        <li><%= msg%></li>
    <% end %>
    </ul>
<% end %>

<%= render 'form' %>